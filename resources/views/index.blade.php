@extends("layout.app")

<section>
  {{-- @section('content')
    @foreach ($datas as $data)
        <p>{{ $data->name }}</p>
        <p>{{ $data->email }}</p>
        <p>{{ $data->gender }}</p>
        <p>{{ $data->des }}</p>
    @endforeach
  @endsection --}}
</section>